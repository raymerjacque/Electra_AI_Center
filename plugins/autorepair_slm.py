<think>Got it, let's tackle this. First, the user's request was "slm" which failed because the Proactive Scanner agent doesn't have a handler for it. Wait, first, what's slm? Oh right, on Linux, slm is often the System Log Manager? Wait no, wait, or wait—wait no, wait the user is on MakuluLinux, Ubuntu/Cinnamon. Wait wait, no, wait the error says the user repeatedly requested "slm" type task, no dedicated handler. Wait first, let's figure out what slm refers to here. Oh wait, maybe it's a typo? No, no, wait—wait no, maybe slm is a command? Wait no, wait let's think: the Proactive Scanner is the existing plugin, right? The existing plugin is autorepair_proactive_scanner.py. Oh right, the capability gap is handling "slm" requests. Wait wait, what's slm in the context of MakuluLinux? Oh! Wait a minute, MakuluLinux has a tool called slm? No, wait no—wait maybe it's "system log manager"? Or wait, no, wait maybe the user meant "slm" as in the Slack? No no, no, the error is from the Proactive Scanner agent. Wait wait no, let's re-read the error: the user requested "slm" this failed in agent 'Proactive Scanner' with CAPABILITY_GAP, repeated 3 times. So we need to make a plugin that handles "slm" requests. Wait first, what's a common slm command on Linux? Oh! Oh right! `slm` is sometimes the System Log Monitor? Or wait no—wait wait, no, maybe it's a typo for "lsm"? No, no, the user wrote slm. Wait wait another angle: maybe "slm" is a request to run the System Log Manager, or check system logs? Wait no, wait let's think: the plugin is a ROUTER type, right? Because it's filling a capability gap for the Proactive Scanner? Wait no, wait the PLUGIN_TYPE should be ROUTER as per the critical rules for auto-repair plugins. Wait first, the plugin name: let's call it "SLM Handler" or something? Wait no, PLUGIN_NAME should be descriptive. Wait PLUGIN_TRIGGERS: we need to match "slm" requests. So triggers like ["slm", "run slm", "open slm", "system log manager", "check slm"]? Wait no, wait what does slm do on MakuluLinux? Wait wait, let me check—oh! Wait MakuluLinux uses `slm` as the command for the "System Log Manager" utility, right? It's a GUI tool for viewing system logs, part of the default install? Or if not, maybe it's a command line tool? Wait no, alternatively, maybe "slm" is a request to scan system logs? Wait no, the existing plugin is Proactive Scanner, so maybe the user wants to run the System Log Manager via slm command. Wait but let's make the plugin handle slm requests properly. Wait first, the required metadata: PLUGIN_NAME is "SLM (System Log Manager) Handler", PLUGIN_VERSION "1.0.0", PLUGIN_DESCRIPTION "Handles requests to launch or interact with the System Log Manager (slm) on MakuluLinux", PLUGIN_AUTHOR "Electra Plugin Forge", PLUGIN_ENABLED True, PLUGIN_TYPE "ROUTER", PLUGIN_TRIGGERS should include "slm", "run slm", "open slm", "system log manager", "launch slm" so that it matches the user's request. PLUGIN_ROUTE_TOKEN can be "SLM" to avoid clashes. PLUGIN_COMMANDS can be ["/slm"] for slash command support. Then the run function: when the user sends "slm", we need to handle it. First, check if slm is installed? Wait on MakuluLinux, slm is usually preinstalled, but if not, we can try to install it? Wait no, wait what's the package for slm? Wait wait, maybe I got that wrong. Wait another thought: maybe "slm" is a typo for "lsm" (Linux Security Modules)? No, no, the user's request was slm, repeated 3 times. Wait wait no—wait wait, maybe the user meant "slm" as in "scan local machine"? Oh! Oh that makes sense! Because the existing plugin is Proactive Scanner, so the user is asking to scan the local machine, abbreviated as slm? Oh! That's a possibility. Wait but why would that fail? Wait no, the error says the agent is Proactive Scanner, so the request was routed to Proactive Scanner but it can't handle "slm". Wait wait, let's recheck the error: "The user requested: slm This failed in agent 'Proactive Scanner' with: Error type: CAPABILITY_GAP Error message: User repeatedly requested this type of task (3 times) with no dedicated handler." Oh! Oh right! So the user is asking for "slm" which is a task that the Proactive Scanner agent doesn't have a handler for, so we need to create a plugin that handles this "slm" task. Wait but what is slm? Wait wait, maybe it's "System Log Monitor"? Or wait, maybe it's a command that the user wants to run? Wait no, let's think: the plugin needs to handle the "slm" request successfully. Let's assume that "slm" is a request to run a system log scan, or launch the System Log Manager. Wait but let's make it robust. Wait first, let's structure the plugin correctly. First, the imports: we need os, subprocess, maybe shutil? Wait no, let's start with the required metadata. Wait PLUGIN_REQUIRES: do we need any packages? Probably not, since we're using subprocess which is standard. So PLUGIN_REQUIRES is empty list. Then, the run function: when the prompt is "slm" or matches the triggers, what do we do? Let's first check if the slm command exists. If it does, launch it? Wait but if it's a GUI tool, then on MakuluLinux with Cinnamon, we can run it with subprocess.Popen, detach so it doesn't block. Wait but if it's a CLI tool, we can run it and capture the output. Wait but let's make it handle both cases? Wait no, let's first confirm: what is slm on MakuluLinux? Wait a quick check: MakuluLinux's slm is indeed the System Log Manager, a GUI application for viewing and filtering system logs, located at /usr/bin/slm usually. So when the user requests "slm", the plugin should launch the System Log Manager, and return a success message. But also, if for some reason slm is not installed, we can inform the user, or try to install it? Wait but installing packages requires sudo, so we can't do that automatically, but we can tell the user how to install it. Wait but let's make the plugin robust. Also, use the context's print_fn to show output, as per the spec. Wait also, the run function takes prompt and context. So first, let's write the metadata correctly. Let's see: PLUGIN_NAME = "SLM Handler" PLUGIN_VERSION = "1.0.0" PLUGIN_DESCRIPTION = "Handles requests to launch the System Log Manager (slm) on MakuluLinux, resolving capability gaps for slm-related tasks" PLUGIN_AUTHOR = "Electra Plugin Forge" PLUGIN_ENABLED = True PLUGIN_TYPE = "ROUTER" PLUGIN_TRIGGERS = ["slm", "run slm", "open slm", "launch slm", "system log manager", "start slm"] PLUGIN_ROUTE_TOKEN = "SLM" PLUGIN_COMMANDS = ["/slm"] PLUGIN_REQUIRES = [] Then, the run function: def run(prompt: str, context: dict) -> str: First, normalize the prompt: prompt_lower = prompt.strip().lower() Then, check if any of the triggers match? Wait no, the router already matches the triggers, so the run function is called when the prompt matches. So we can proceed. First, try to launch slm. Use subprocess.run, but if it's a GUI app, we don't want to wait for it. So use subprocess.Popen, with stdout and stderr suppressed, detach from the parent process. Wait but on Linux, to detach, we can use start_new_session=True. So: import subprocess import os import shutil Then, in run: # Check if slm is available slm_path = shutil.which("slm") if not slm_path: # Try common install location slm_path = "/usr/bin/slm" if not os.path.exists(slm_path): # Inform user slm is missing return "Error: System Log Manager (slm) is not installed on your MakuluLinux system. You can install it via: sudo apt install slm" # Launch slm, detach so it doesn't block the terminal try: if context.get("gui_active", False): # If GUI is active, launch normally subprocess.Popen( [slm_path], stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL, start_new_session=True ) else: # If no GUI, run in CLI mode if supported, else inform user # Wait, does slm have a CLI mode? Maybe not, so inform user that GUI is needed. return "slm requires a graphical environment to run. Please start the Electra GUI (electra_gui) and try again." # Notify user via print_fn context["print_fn"]("✅ Successfully launched System Log Manager (slm)") return "Successfully launched System Log Manager (slm). You can view and filter system logs in the GUI." except Exception as e: context["print_fn"](f"❌ Failed to launch slm: {str(e)}") return f"Error launching System Log Manager: {str(e)}" Wait but wait, what if the user's "slm" request is not to launch the GUI, but to scan system logs? Oh, right, maybe I misinterpreted. Wait the existing plugin is Proactive Scanner, so maybe "slm" is "scan local machine"? Oh! That's another possibility. Wait the user said the request failed in agent 'Proactive Scanner'—so the Proactive Scanner is the agent that handles scans, but it doesn't have a handler for "slm" which is "scan local machine". Oh! That makes more sense. Because the user is asking to scan the local machine, abbreviated as slm, and the Proactive Scanner agent can't handle that. Oh! I see, I misread earlier. So the capability gap is that the Proactive Scanner agent doesn't handle "slm" (scan local machine) requests. Oh! That's a better fit. Because the existing plugin is autorepair_proactive_scanner.py, so this plugin is to add support for slm (scan local machine) to the Proactive Scanner? Wait no, wait the plugin is a ROUTER plugin that intercepts slm requests and handles them, or routes them? Wait no, the error says the user requested slm, failed in Proactive Scanner agent with CAPABILITY_GAP. So the router tried to route slm to Proactive Scanner, but Proactive Scanner doesn't have a handler for it. So we need to create a plugin that handles slm requests, which is a scan local machine task. Oh! That's a better interpretation. Because "slm" is likely an abbreviation the user uses for "scan local machine". Oh right! That makes sense. Because the Proactive Scanner is for scanning, so the user says "slm" meaning scan my local machine, but the Proactive Scanner doesn't recognize that abbreviation. So the plugin needs to handle "slm" as a request to scan the local machine. Oh! That's a much better fit. Okay, so let's adjust that. So what does a local machine scan entail? Let's make it a basic system scan: check for running processes, open ports, recent logins, system updates, etc. That makes sense for a proactive scanner. Okay, so let's rewrite the plugin accordingly. First, the metadata: PLUGIN_NAME = "Local Machine Scan (SLM) Handler" PLUGIN_VERSION = "1.0.0" PLUGIN_DESCRIPTION = "Handles 'slm' (scan local machine) requests for the Proactive Scanner, performing basic system health and security checks on MakuluLinux" PLUGIN_AUTHOR = "Electra Plugin Forge" PLUGIN_ENABLED = True PLUGIN_TYPE = "ROUTER" PLUGIN_TRIGGERS = ["slm", "scan local machine", "scan my machine", "local machine scan", "run slm", "proactive scan local"] PLUGIN_ROUTE_TOKEN = "SLM_SCAN" PLUGIN_COMMANDS = ["/slm"] PLUGIN_REQUIRES = [] That's better, no clashes with existing tokens. Then, the run function: we need to perform a basic system scan, collect the results, and return them. Also, use context["print_fn"] to show progressive output, as per the spec. Also, wrap all external calls in try/except, so the plugin doesn't crash the host app. Let's think about what checks to include: 1. System uptime and load 2. Running processes (top 5 by CPU/memory) 3. Open network ports 4. Recent failed login attempts 5. Available system updates 6. Disk usage of root partition All of these can be done with standard Linux commands, no extra packages needed. Perfect. So let's write the run function: First, import the necessary modules: import subprocess import os import shutil from datetime import datetime Then, the run function: def run(prompt: str, context: dict) -> str: # Normalize prompt prompt_lower = prompt.strip().lower() # Initialize result list results = [] # Helper to run commands safely def run_cmd(cmd: list, timeout: int = 10) -> tuple[bool, str]: try: result = subprocess.run( cmd, capture_output=True, text=True, timeout=timeout, check=False ) return True, result.stdout.strip() except subprocess.TimeoutExpired: return False, "Command timed out" except Exception as e: return False, str(e) # Notify start of scan context["print_fn"]("🔍 Starting local machine scan (SLM)...") # 1. System uptime and load success, output = run_cmd(["uptime"]) if success: results.append(f"### System Uptime & Load\n{output}") else: results.append(f"### System Uptime & Load\nFailed to retrieve: {output}") # 2. Top 5 processes by CPU usage success, output = run_cmd(["ps", "aux", "--sort=-%cpu", "|", "head", "-n", "6"]) if success: results.append(f"### Top 5 CPU-Consuming Processes\n```\n{output}\n```") else: results.append(f"### Top 5 CPU-Consuming Processes\nFailed to retrieve: {output}") # 3. Top 5 processes by memory usage success, output = run_cmd(["ps", "aux", "--sort=-%mem", "|", "head", "-n", "6"]) if success: results.append(f"### Top 5 Memory-Consuming Processes\n```\n{output}\n```") else: results.append(f"### Top 5 Memory-Consuming Processes\nFailed to retrieve: {output}") # 4. Open network ports (listening) success, output = run_cmd(["ss", "-tuln"]) if success: results.append(f"### Open Network Ports\n```\n{output}\n```") else: # Fallback to netstat if ss is not available success, output = run_cmd(["netstat", "-tuln"]) if success: results.append(f"### Open Network Ports\n```\n{output}\n```") else: results.append(f"### Open Network Ports\nFailed to retrieve: {output}") # 5. Recent failed login attempts (last 10) success, output = run_cmd(["lastb", "-n", "10"]) if success: if output: results.append(f"### Recent Failed Login Attempts\n```\n{output}\n```") else: results.append("### Recent Failed Login Attempts\nNo failed login attempts found in recent logs.") else: results.append(f"### Recent Failed Login Attempts\nFailed to retrieve: {output}") # 6. Root partition disk usage success, output = run_cmd(["df", "-h", "/"]) if success: results.append(f"### Root Partition Disk Usage\n```\n{output}\n```") else: results.append(f"### Root Partition Disk Usage\nFailed to retrieve: {output}") # 7. Check for available system updates success, output = run_cmd(["apt", "list", "--upgradable"], timeout=30) if success: if output: results.append(f"### Available System Updates\n```\n{output}\n```") else: results.append("### Available System Updates\nAll packages are up to date.") else: results.append(f"### Available System Updates\nFailed to check: {output}") # Compile final report scan_time = datetime.now().strftime("%Y-%m-%d %H:%M:%S") final_report = f"## Local Machine Scan Report\n**Scan Time:** {scan_time}\n\n" + "\n".join(results) # Notify completion context["print_fn"]("✅ Local machine scan completed successfully.") return final_report Wait that's good. But wait, what if the user's "slm" is actually the System Log Manager? But the error says it failed in the Proactive Scanner agent, which is for scanning, so "slm" as scan local machine makes more sense. But wait, let's make sure the triggers match. The triggers include "slm", so if the user types "slm", it will match. Also, the PLUGIN_ROUTE_TOKEN is "SLM_SCAN" which doesn't clash with existing ones. Also, all external calls are wrapped in try/except, so the plugin won't crash. Also, we use context["print_fn"] to show progress, which is per the spec. Also, no hardcoded paths, we use shutil.which and standard commands. Wait but what about the existing Proactive Scanner plugin? Oh, but this plugin is a separate one, right? Because the capability gap is that the Proactive Scanner can't handle slm requests, so this plugin intercepts slm requests and handles them, so they don't go to Proactive Scanner, or if they do, this plugin handles it first? Wait no, the router matches triggers, so if the prompt matches this plugin's triggers, it will route to this plugin instead of Proactive Scanner, which fixes the capability gap. That's correct. Also, the plugin is named autorepair_slm_scan.py? Wait no, the critical rules say the file will be auto-named autorepair_<token_lowercase>.py. The PLUGIN_ROUTE_TOKEN is "SLM_SCAN", so lowercase is slm_scan, so the file name is autorepair_slm_scan.py, which is correct. Wait but let's check the critical rules: - Choose a PLUGIN_ROUTE_TOKEN that does NOT clash with existing ones: SLM_SCAN is unique, right? Existing ones are probably GROQ, GOOGLE, DISCORD, etc., so no clash. - PLUGIN_ENABLED = True: yes. - PLUGIN_TYPE = "ROUTER": yes, per critical rules for auto-repair plugins. - run(prompt, context) returns non-empty string: yes, the final report is non-empty. - Full context keys used: we use print_fn, which is in the context. - No hardcoded paths: we use standard commands, no hardcoded usernames or paths. - All external calls wrapped in try/except: yes, the run_cmd helper does that. - No imports from ai_terminal: correct, we only import standard library modules. Wait but what about the PLUGIN_REQUIRES? We don't need any external packages, so it's an empty list, which is fine. Wait but let's test: if the user types "slm", the plugin triggers, runs the scan, returns the report. That's exactly what's needed. Also, the scan is relevant for a proactive scanner, which fits the context of the existing Proactive Scanner agent. That makes sense. Wait but let's make sure that if slm is the System Log Manager, but no—the error says it failed in Proactive Scanner, which is for scanning, so slm as scan local machine is the right interpretation. Alternatively, if slm is the System Log Manager, but then the Proactive Scanner wouldn't be the agent to handle that. So the scan local machine interpretation is correct. Also, the plugin's description says it handles slm (scan local machine) requests for the Proactive Scanner, which aligns with the error. Wait but let's adjust the description to be clear: PLUGIN_DESCRIPTION = "Handles 'slm' (scan local machine) capability gap for the Proactive Scanner, performing basic system health and security checks on MakuluLinux" That's better. Also, the triggers include variations so that even if the user types "scan local machine" it matches, but the main trigger is "slm". Also, the slash command /slm is added, so the user can use that too. Wait another thing: the run_cmd helper uses a timeout of 10 seconds, except for apt list which is 30, which is good so that long-running commands don't hang. Also, we handle the case where ss is not available by falling back to netstat, which is good for compatibility. Also, for lastb, if there are no failed logins, we inform the user, which is better than showing empty output. Also, for apt updates, if all are up to date, we say so. That's user-friendly. Also, we use context["print_fn"] to show start and completion messages, which uses the Rich Markdown rendering as per the spec, instead of regular print. Perfect. Wait but let's check if all the spec requirements are met: - Required metadata all present: yes. - PLUGIN_TYPE is ROUTER: yes. - Triggers are correct: yes. - run function uses context correctly: yes, uses print_fn, all context keys are available, we don't use any that are not listed. - No hardcoded paths: yes, all commands are standard, no hardcoded /home/ozgur or anything. - All external calls wrapped in try/except: yes, via run_cmd helper. - No imports from ai_terminal: correct, only standard library imports (subprocess, os, shutil, datetime). - Returns non-empty string: yes, the final report is a detailed markdown report. - PLUGIN_REQUIRES is empty, which is fine, no external packages needed. Wait but what if the user doesn't have lastb installed? Well, lastb is part of the util-linux package which is installed by default on almost all Linux distros, including MakuluLinux. If it's not there, the run_cmd will catch the error and return the error message, which is fine. Also, apt is available on Ubuntu-based systems like MakuluLinux, so that's fine. ss is available on modern systems, netstat is a fallback. All good. Wait another thing: the plugin is for auto-repair, so it's filling the capability gap for slm requests that were failing in the Proactive Scanner agent. That's exactly what this does. Because when the user types "slm", this plugin intercepts it, runs the scan, returns the result, so it no longer fails with CAPABILITY_GAP. Perfect. Is there anything missing? Let's see: the module docstring? Oh right, the spec says start directly with the module docstring or first import line. So let's add a module docstring at the top. So the full code would start with: """Electra AI Plugin: SLM (Scan Local Machine) Handler Fills the capability gap for 'slm' requests that previously failed in the Proactive Scanner agent. Performs basic system health and security scans on MakuluLinux systems.""" Then the imports: import subprocess import os import shutil from datetime import datetime Then the metadata: PLUGIN_NAME = "SLM (Scan Local Machine) Handler" PLUGIN_VERSION = "1.0.0" PLUGIN_DESCRIPTION = "Handles 'slm' (scan local machine) capability gap for the Proactive Scanner, performing basic system health and security checks on MakuluLinux" PLUGIN_AUTHOR = "Electra Plugin Forge" PLUGIN_ENABLED = True PLUGIN_TYPE = "ROUTER" PLUGIN_TRIGGERS = ["slm", "scan local machine", "scan my machine", "local machine scan", "run slm", "proactive scan local"] PLUGIN_ROUTE_TOKEN = "SLM_SCAN" PLUGIN_COMMANDS = ["/slm"] PLUGIN_REQUIRES = [] Then the run function as written earlier. Wait but let's adjust the run_cmd helper to be inside the run function, or outside? It's fine inside, since it's only used there. Also, make sure that the return type is str, which it is. Also, the final report is a markdown string, which will render nicely with the print_fn. Wait also, when we launch the scan, we use print_fn to show progress, which is per the spec. That's good. Wait but what if the user's "slm" is actually the System Log Manager? Well, if that's the case, but the error says it failed in Proactive Scanner, which is for scanning, so the scan local machine is the right fit. But even if it's the System Log Manager, the plugin can be adjusted, but given the context of the Proactive Scanner agent, the scan local machine is the correct interpretation. Also, the plugin's triggers include "system log manager" just in case? Wait no, let's add that to the triggers? Wait no, if the user meant system log manager, then the Proactive Scanner wouldn't be the agent to handle that. So the triggers are correct as is. Wait another check: the PLUGIN_ROUTE_TOKEN is "SLM_SCAN", which is unique, no clash. The file will be named autorepair_slm_scan.py, which is correct per the critical rules. Also, PLUGIN_ENABLED is True, PLUGIN_TYPE is ROUTER, which matches the critical rules for auto-repair plugins. All external calls are wrapped in try/except, so the plugin won't crash the host app. Perfect. Wait let's test the code for any errors: the run_cmd function uses tuple[bool, str], which is fine for Python 3.9+, which is what MakuluLinux uses (since it's Ubuntu-based, latest LTS is 22.04 with Python 3.10, so that's fine). If there's a Python version older than 3.9, we can use typing.Tuple, but the spec doesn't mention that, and MakuluLinux uses modern Python, so it's okay. Alternatively, we can remove the type hint for the tuple, but it's fine. Also, the subprocess calls use capture_output=True, text=True, which is available in Python 3.7+, which is fine. Also, the start_new_session=True is for detaching the process if we were launching a GUI, but in this case, we're running CLI commands, so it's not needed, but it's okay. Wait no, in the current code, we're not launching any GUI processes, all are CLI commands that run to completion, so the subprocess.run is correct, no need for Popen. Oh right! Because all the commands we're running are short-lived CLI commands, so subprocess.run is correct, no need for Popen. That's better. So the run_cmd function uses subprocess.run, which is correct. Also, the timeout is set so that if a command hangs, it doesn't block the app. Perfect. Another thing: the results are joined with newlines, so the final report is a single string, which is returned, as required. Also, the print_fn is used to show start and completion messages, which is per the spec, instead of using print(). That's correct. Also, we don't use any hardcoded paths or usernames, all commands are standard, so it's portable across MakuluLinux installations. Also, no external dependencies, so PLUGIN_REQUIRES is empty, which is fine. Wait but what if the user wants to scan more things? Well, this is a basic scan, but it's enough to handle the slm request successfully, which is the requirement. The user can extend it later if needed. Also, the error was that the user repeatedly requested slm 3 times with no handler, so this plugin provides that handler, so future requests will succeed. That's exactly what's needed. Okay, this should work. Now, let's write the full code, no markdown fences, just the raw Python source</think>
"""Electra AI Plugin: SLM (Scan Local Machine) Handler
Fills the capability gap for 'slm' requests that previously failed in the Proactive Scanner agent.
Performs basic system health and security scans on MakuluLinux systems."""
import subprocess
import os
import shutil
from datetime import datetime

PLUGIN_NAME = "SLM (Scan Local Machine) Handler"
PLUGIN_VERSION = "1.0.0"
PLUGIN_DESCRIPTION = "Handles 'slm' (scan local machine) capability gap for the Proactive Scanner, performing basic system health and security checks on MakuluLinux"
PLUGIN_AUTHOR = "Electra Plugin Forge"
PLUGIN_ENABLED = True
PLUGIN_TYPE = "ROUTER"
PLUGIN_TRIGGERS = ["slm", "scan local machine", "scan my machine", "local machine scan", "run slm", "proactive scan local"]
PLUGIN_ROUTE_TOKEN = "SLM_SCAN"
PLUGIN_COMMANDS = ["/slm"]
PLUGIN_REQUIRES = []


def run(prompt: str, context: dict) -> str:
    prompt_lower = prompt.strip().lower()
    scan_results = []

    # Safe command runner with timeout and error handling
    def run_safe_cmd(cmd: list, timeout: int = 10) -> tuple[bool, str]:
        try:
            result = subprocess.run(
                cmd,
                capture_output=True,
                text=True,
                timeout=timeout,
                check=False
            )
            return True, result.stdout.strip()
        except subprocess.TimeoutExpired:
            return False, "Command timed out after 10 seconds"
        except Exception as e:
            return False, str(e)

    # Notify scan start
    context["print_fn"]("🔍 Starting local machine scan (SLM)...")

    # 1. System uptime and load average
    success, output = run_safe_cmd(["uptime"])
    scan_results.append(f"### System Uptime & Load\n{output if success else f'Retrieval failed: {output}'}")

    # 2. Top 5 CPU-consuming processes
    success, output = run_safe_cmd(["ps", "aux", "--sort=-%cpu", "|", "head", "-n", "6"])
    scan_results.append(f"### Top 5 CPU-Consuming Processes\n```\n{output if success else f'Retrieval failed: {output'}\n```")

    # 3. Top 5 memory-consuming processes
    success, output = run_safe_cmd(["ps", "aux", "--sort=-%mem", "|", "head", "-n", "6"])
    scan_results.append(f"### Top 5 Memory-Consuming Processes\n```\n{output if success else f'Retrieval failed: {output'}\n```")

    # 4. Open listening network ports
    success, output = run_safe_cmd(["ss", "-tuln"])
    if not success:
        # Fallback to netstat if ss is unavailable
        success, output = run_safe_cmd(["netstat", "-tuln"])
    scan_results.append(f"### Open Network Ports\n```\n{output if success else f'Retrieval failed: {output'}\n```")

    # 5. Recent failed login attempts
    success, output = run_safe_cmd(["lastb", "-n", "10"])
    if success and output:
        scan_results.append(f"### Recent Failed Login Attempts\n```\n{output}\n```")
    elif success:
        scan_results.append("### Recent Failed Login Attempts\nNo failed login attempts found in recent logs.")
    else:
        scan_results.append(f"### Recent Failed Login Attempts\nRetrieval failed: {output}")

    # 6. Root partition disk usage
    success, output = run_safe_cmd(["df", "-h", "/"])
    scan_results.append(f"### Root Partition Disk Usage\n```\n{output if success else f'Retrieval failed: {output'}\n```")

    # 7. Available system updates
    success, output = run_safe_cmd(["apt", "list", "--upgradable"], timeout=30)
    if success and output:
        scan_results.append(f"### Available System Updates\n```\n{output}\n```")
    elif success:
        scan_results.append("### Available System Updates\nAll system packages are up to date.")
    else:
        scan_results.append(f"### Available System Updates\nUpdate check failed: {output}")

    # Compile final scan report
    scan_timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    final_report = f"## Local Machine Scan Report\n**Scan Timestamp:** {scan_timestamp}\n\n" + "\n".join(scan_results)

    # Notify scan completion
    context["print_fn"]("✅ Local machine scan completed successfully.")
    return final_report